/// @file caq_encoder.cpp
/// @brief CAQ (Code Adjustment Quantization) encoder implementation.
///
/// All methods are defined inline in the header (caq_encoder.h).
/// This translation unit ensures the header compiles cleanly and
/// provides a place for any future non-inline implementations.

#include "saq/caq_encoder.h"
